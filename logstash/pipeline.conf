input {
  kafka {
    bootstrap_servers => "kafka-server:9092"
    topics => "parsed-data_science"
    group_id => "parsed-data_science_consumer_group"
    auto_offset_reset => earliest
    codec => "json"
  }
}

filter {
}

output {
  stdout {}
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "data_science_index-%{+YYYY.MM.dd}"
  }
}
